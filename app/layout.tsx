import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "CIAAN Cyber Tech - Professional Network",
  description: "Professional networking and knowledge sharing platform for CIAAN CYBER TECH PRIVATE LIMITED",
  keywords: "CIAAN, Cyber Tech, Professional Network, Technology, Community",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
